[package]
name = "c-staticlib"
publish = false
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["staticlib"]

[dependencies]
foundation = { workspace = true, features = ["std", "libc-main"] }
spike-platform = { workspace = true, features = ["std"] }
